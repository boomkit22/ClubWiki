spring:
  datasource:
    url: jdbc:mysql://localhost:3306/club?serverTimezone=UTC&characterEncoding=UTF-8
    username: root
    password: qkd077023!
    driver-class-name: com.mysql.cj.jdbc.Driver


  jpa:
    hibernate:
      ddl-auto: create #create or none
    properties:
      hibernate:
#        show_sql: true
        format_sql: true

logging.level:
  org.hibernate.SQL: debug



# org.hibernate.type: trace

server:
  port: 8081

##git ignore test